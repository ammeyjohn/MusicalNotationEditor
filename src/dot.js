'use strict';

import Symbol from './symbol.js';

export default class Dot extends Symbol {
    constructor() {
        super('dots.dot');
    }

    draw(g, x, y) {
        return super.draw(g, x, y);
    }
}